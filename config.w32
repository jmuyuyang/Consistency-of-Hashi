// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("consistent", "for consistent support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("consistent", "enable consistent support", "no");

if (PHP_CONSISTENT != "no") {
	EXTENSION("consistent", "consistent.c");
}

